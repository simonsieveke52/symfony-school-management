<!DOCTYPE html>
<html>
    <head>
           <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
         <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
    <title>  {% block title %}Welcome| NajmiAccademy.com{% endblock %}</title>
        {% block stylesheets %}
{{ encore_entry_link_tags('app')}}
        {% endblock %}
    </head>
    <body>

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">NajmiAccademy</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/" data-toggle="tooltip" data-placement="top" title="Home"><i class="fas fa-home"></i>Home <span class="sr-only">(current)</span></a>
      </li>

      {% if is_granted("ROLE_PROF") %}
         <li class="nav-item">
           <a class="nav-link" href="{{ path('prof_add_course') }}"><i class="fas fa-plus"></i>Cours</a>
        </li>
         <li class="nav-item">
           <a class="nav-link" href="{{ path('prof_add_anounce') }}"><i class="fas fa-bullhorn"></i>Anounce</a>
        </li>

         <li class="nav-item">
           <a class="nav-link" href="{{ path('prof') }}"><i class="fas fa-bullhorn"></i>Page Principale</a>
        </li>

         </ul>
   
      
     
     
     </ul>  
  <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle  align-top" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
         <img src="{{ app.user.picture }}"  class="avatar avatar-mini" 
         alt="{{ app.user.name }}" title="{{ app.user.name }}" style="border-radius:100px" width="90px" height="90px">
        </a>
    
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="{{  path('prof_profile',{'id': app.user.id}) }}">Mon compte</a>

           <a class="dropdown-item" href="{{ path('prof_synthese', {'id':app.user.id })}}">Etudiants</a>
           <a class="dropdown-item" href="{{ path('prof_add_course')}}">Ajouter un cours</a>
           <a class="dropdown-item" href="{{ path('prof_add_anounce')}}">Ajouter une annonce</a>
            <a class="dropdown-item" href="{{ path('prof_update_password')}}">Editer Password</a>
           <a class="dropdown-item" href="{{ path('app_prof_logout') }}">Déconnexion</a>
            </div>
      </li>
    </ul>
            {% endif %}


  </div>

</nav>

        {% block body %}{% endblock %}
       
           
        {% include 'partials/footer.html.twig' %}  

           {% block javascripts %}
       {{ encore_entry_script_tags('app')}}
           {% endblock %}

    </body>

</html>

{% extends 'base.html.twig' %}

{% block title %}Contact| NAjmiAccademy.com{% endblock %}

{% block body %}

   <div class="container bg-light p-5 mt-5" style="max-width:450px;">

   	{{ form_start(contactForm)}}
     <fieldset>
    <legend class="text-center w-10"
         style="box-shadow: 1px 2px skyblue; margin-bottom:10px;" 
    > Hi M'envoyer un Message !</legend>
   <div class="form-group">
   		{{ form_row(contactForm.email,{'attr':{'placeholder': 'Entrer votre email'}})}}
   </div>
   <div class="form-group">
   	{{ form_row(contactForm.contennt, {'attr':{'placeholder': 'Dit moi quelque chose'}})}}
   </div>
   
 
 <button class="btn btn-primary text-center" type="submit">
 	Envoyer
 </button>
</fieldset>
   	{{ form_end(contactForm)}}
   	
   </div>
{% endblock %}
